OBJ    = shell.o
GRAPHS = shell-bash.csv shell-prog.csv
PROG   = shell
PROGS  = shell-program
TMP    = tmp.dat
CFLAGS = 

include ../lib/lib.mk

shell-program: shell-program.c
	$(CC) -o shell-program shell-program.c $(CFLAGS)
